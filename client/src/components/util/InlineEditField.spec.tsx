import { act, render, screen, waitFor, within } from "@testing-library/react";

describe("components", () => {
  describe("util", () => {
    describe("InlineEditField", () => {
      it.todo("displays a label and button if not edittable");

      it.todo("allows editting and cancelling of the edit");

      it.todo("allows editting and saving of the edit with validation errors");

      it.todo("allows editting and saving of the edit successfully");
    });
  });
});
