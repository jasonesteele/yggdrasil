import { setWindowWidth } from "../util/test-utils";

describe("components", () => {
  describe("UserProfileButton", () => {
    beforeAll(() => {
      setWindowWidth(1024);
    });

    it.todo("renders the component");
  });
});
