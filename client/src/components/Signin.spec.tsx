import { setWindowWidth } from "../util/test-utils";

describe("components", () => {
  describe("Signin", () => {
    beforeAll(() => {
      setWindowWidth(1024);
    });

    it.todo("renders the component");
  });
});
