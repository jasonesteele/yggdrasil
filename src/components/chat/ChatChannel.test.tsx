import { fail, setWindowWidth } from "../../util/test-utils";

describe("components", () => {
  describe("ChatChannel", () => {
    beforeAll(() => {
      setWindowWidth(1024);
    });

    it("renders", () => {
      fail("not implemented");
    });
  });
});
