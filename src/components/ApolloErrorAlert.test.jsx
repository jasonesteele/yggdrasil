import { setWindowWidth, fail } from "../util/test-utils";

describe("components", () => {
  describe("ApolloErrorAlert", () => {
    beforeAll(() => {
      setWindowWidth(1024);
    });

    it("renders", () => {
      fail("not implemented");
    });
  });
});
